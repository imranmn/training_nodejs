// for loop

for(let i = 0; i <= 10; i++) {
    console.log("i =", i);
}

// foreach loop - khas untuk array
// => arrow function
[1, 2, 3, 4, 5].forEach((item, index) => {
    console.log("item =", item, index);
});
